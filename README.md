# وب اسکرپر پایتون

یک اسکرپر ساده و کاربردی با پایتون برای بازدید و استخراج اطلاعات از صفحات وب.

## نصب وابستگی‌ها

```bash
pip install -r requirements.txt
```

## نحوه استفاده

### روش اول: اجرای مستقیم
```bash
python scraper.py
```

### روش دوم: اجرای مثال
```bash
python example_multi_visit.py
```

سپس آدرس صفحه وب مورد نظر را وارد کنید.

### روش سوم: استفاده در کد خود

```python
from scraper import WebScraper

# create scraper
scraper = WebScraper("https://example.com")

# visit page
if scraper.visit_page():
    # get title page
    title = scraper.get_title()
    print(f"عنوان: {title}")
    
    # get links
    links = scraper.get_links()
    print(f"تعداد لینک‌ها: {len(links)}")
    
    # get images
    images = scraper.get_images()
    print(f"تعداد تصاویر: {len(images)}")
    
    # save HTML
    scraper.save_html("output.html")
```

## امکانات

- ✅ بازدید از صفحات وب
- ✅ استخراج عنوان صفحه
- ✅ استخراج تمام لینک‌ها
- ✅ استخراج تمام تصاویر
- ✅ استخراج متن صفحه
- ✅ ذخیره HTML صفحه
- ✅ مدیریت خطاها
- ✅ شبیه‌سازی مرورگر واقعی
- 🆕 **بازدید چندباره با تاخیر تصادفی** (ضد ربات)
- 🆕 **User-Agent های متنوع و تصادفی**
- 🆕 **تاریخچه بازدیدها**
- 🆕 **آمار و گزارش‌گیری**

## مثال‌های کاربردی

### مثال 1: استخراج لینک‌های یک صفحه
```python
scraper = WebScraper("https://news.ycombinator.com")
if scraper.visit_page():
    links = scraper.get_links()
    for link in links:
        print(link)
```

### مثال 2: ذخیره محتوای صفحه
```python
scraper = WebScraper("https://example.com")
if scraper.visit_page():
    scraper.save_html("example.html")
    text = scraper.get_text()
    print(text)
```

### مثال 3: بازدید چندباره (ضد ربات) 🆕
```python
scraper = WebScraper("https://example.com")

# visit 10 time with delay 5 until 15 seconds
stats = scraper.visit_multiple_times(
    count=10,              # number of visits
    min_delay=5,           # minimium delay 
    max_delay=15,          # max delay
    random_agent=True      # use of random User-Agent
)

print(f"موفق: {stats['success']}/{stats['total']}")
print(f"نرخ موفقیت: {stats['success_rate']:.1f}%")
```

## ویژگی‌های ضد ربات 🛡️

این اسکرپر برای جلوگیری از شناسایی به عنوان ربات:

### 1. تاخیر تصادفی
- بین هر بازدید یک تاخیر تصادفی اعمال می‌شود
- تاخیر قابل تنظیم (مثلاً 3 تا 10 ثانیه)
- شبیه‌سازی رفتار انسانی

### 2. User-Agent متنوع
- 7 User-Agent مختلف از مرورگرهای مختلف
- انتخاب تصادفی در هر درخواست
- شامل Chrome، Firefox، Safari، Edge

### 3. هدرهای کامل HTTP
- شبیه‌سازی کامل درخواست مرورگر
- Accept، Accept-Language، Accept-Encoding
- DNT، Connection، Upgrade-Insecure-Requests

### 4. تاریخچه و آمار
- ثبت تمام بازدیدها با زمان دقیق
- گزارش موفقیت/شکست
- نرخ موفقیت

## توجه ⚠️

- از این اسکرپر فقط برای سایت‌هایی استفاده کنید که اجازه اسکرپ می‌دهند
- به قوانین `robots.txt` سایت‌ها احترام بگذارید
- از فواصل زمانی مناسب بین درخواست‌ها استفاده کنید
- استفاده نادرست ممکن است منجر به مسدود شدن IP شما شود
